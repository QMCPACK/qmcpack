name: static

on:
  workflow_dispatch:
    branches:
      - develop
      - main

jobs:
  linux:
    runs-on: ubuntu-latest
    container: ${{ matrix.container }}
    env:
      GH_JOBNAME: ${{ matrix.jobname }}
      GH_OS: Linux
    strategy:
      fail-fast: false
      matrix:
        jobname: [ClangTidy22-NoMPI-Real, ClangTidy22-NoMPI-Complex]
        include:
          - jobname: ClangTidy22-NoMPI-Real
            container:
              image: ghcr.io/qmcpack/ci-ubuntu26-openmpi:latest
              options: -u 1001

          - jobname: ClangTidy22-NoMPI-Complex
            container:
              image: ghcr.io/qmcpack/ci-ubuntu26-openmpi:latest
              options: -u 1001

    steps:
      - name: Checkout Action
        uses: actions/checkout@v7

      - name: Configure
        run: tests/test_automation/github-actions/ci/run_step_static.sh configure

      - name: Build
        run: tests/test_automation/github-actions/ci/run_step_static.sh build
