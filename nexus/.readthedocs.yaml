version: 2

build:
  os: ubuntu-26.04
  tools:
    python: "3.14"

sphinx:
  configuration: nexus/docs/conf.py

python:
  install:
    - method: uv
      command: sync
      groups:
        - docs
        - dev
      extras:
        - full
      path: .
